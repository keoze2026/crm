/**
 * The brand blue as a JS value, for SVG strokes and Recharts series. Keep in step with
 * `--color-brand` in index.css — the classes (bg-brand, text-brand…) read the CSS token.
 */
export const BRAND = '#1f4e8c'
export const BRAND_DARK = '#173d70'
