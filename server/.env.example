# # PostgreSQL connection
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=crm
# DB_USER=postgres
# DB_PASSWORD=Rocky544#

# # App
# APP_ENV=development
# CORS_ORIGIN=http://localhost:5173

DB_HOST=127.0.0.1
DB_PORT=5434
DB_NAME=crm
DB_USER=crm_user
DB_PASSWORD=crmKeozx@2026.

# # Auth (passwordless Google Authenticator / TOTP + roles + audit log)
# # Master toggle. false (or unset) => auth is completely bypassed and the app behaves
# # exactly as it did before this feature. true => login is enforced.
# AUTH_ENABLED=false
# # When AUTH_ENABLED=true, CORS is locked to this exact origin (credentials require it).
# CORS_ORIGIN=http://localhost:5173
# # Base URL of the frontend, used to build enrolment links in database/seed_admin.php.
# CLIENT_URL=http://localhost:5173
